# Completed through v0.4

- Native C17 lexer, parser, AST and QVM
- Typed QIR
- Deterministic QBC
- Capability derivation
- Deny-by-default execution guard
- Source-level `requires` declaration
- HMAC-SHA-256 authenticated approval token
- Source scope, expiry and tamper validation
- Approval-token digest in execution receipts

# Remaining stages

1. Ed25519 public-key approval signatures
2. Revocation and replay ledger
3. CPU SIMD backend
4. GPU backend interface
5. Model routing IR and adapters
6. Robotics/device adapters
7. Signed package format and reproducible release pipeline
